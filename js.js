let arr = ['a', 'b', 'c', 'd', 'e'];
for (let i = 1; i <= arr.length - 2; i++) { 
	console.log(arr[i]);
}

let arr1 = ['a', 'b', 'c', 'd', 'e'];
for (let i = arr1.length - 1; i > 0; 
	i--) { 
	console.log(arr1[i]);
}
